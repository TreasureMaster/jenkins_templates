#!groovy
// запрет старта параллельных заданий
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        label 'master'
    }
    options {
        buildDiscarder(logRotator(numToKeppStr: '10', atrifactNumToKeepStr: '10'))
        timestamps()
    }
    stages {
        stage('First step') {
            steps {
                sh 'ssh root@ubuntu23 \'hostname\''
            }
        }
    }
}